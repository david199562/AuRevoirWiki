// JS is sure pretty ugly :(

/**
 * @param {string} pat
 */
var UserAccount = function (pat) {
    this._pat = pat;
};

Account.prototype.get = function(cacheId) {
    // body...
};

Account.prototype.edit = function(cacheId, newContent) {
    // body...
};

Account.prototype.create = function() {
    return ""
};

Account.prototype.delete = function(cacheId) {
    // body...
};
